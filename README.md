# RootProof

**Prove the root cause. Reproduce the failure. Validate the fix.**

RootProof is a terminal-first debugging tool written in Rust.

The goal is to investigate production failures, generate root-cause
hypotheses, reproduce suspected failures, and validate candidate fixes.

## Status

RootProof is currently under active development.

## Development

```bash
cargo check --workspace
cargo test --workspace